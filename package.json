{
  "name": "agent-extensions",
  "license": "MIT",
  "private": true,
  "workspaces": ["plugins/*", "packages/*"],
  "scripts": {
    "test": "cd packages/soda && bun test",
    "typecheck": "cd packages/soda && tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
