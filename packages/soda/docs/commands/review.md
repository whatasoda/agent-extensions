### sd review

#### sd review detect-base-branch
Detects the base branch for the current git branch. Returns JSON with branch information.

Delegates to `scripts/detect-base-branch.ts`.

Example:
```sh
sd review detect-base-branch
```
